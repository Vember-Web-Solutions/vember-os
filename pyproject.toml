[project]
name = "vember-os"
version = "0.1.0"
description = "A terminal-based operational environment with local AI node integration."
readme = "README.md"
requires-python = ">=3.13"
license = { text = "MIT" }
authors = [
    { name = "Vember Web Solutions" }
]
dependencies = [
    "rich",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"